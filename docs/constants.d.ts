export declare abstract class Constants {
    static readonly REPO_URL: string;
    static readonly REPO_NAME: string;
    static readonly REPO_OWNER: string;
    static readonly POST_DIR: string;
    static readonly TOKEN_REGEX: string;
}
